import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {    
    domains: ['people237.com','client.cynomedia-africa.com'], // Ajoutez ici le domaine de votre image
  },
  
};

export default nextConfig;
